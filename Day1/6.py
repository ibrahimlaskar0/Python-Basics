#Hypotenuse of a right angle right

import math

a = float (input ("Enter side A :  "))
b = float(input("Enter side B : "))

c = math.sqrt (pow(a ,2) + pow(b,2))


print(f" the hypotenuse of the right angle triangle is {round(c , 2)}")